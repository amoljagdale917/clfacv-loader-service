spring.application.name=clfacv-loader

# Shared defaults. Environment-specific datasource lives in:
# - application-postprod.yml
# - application-prod.yml
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
